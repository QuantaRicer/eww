(defpoll time :interval "2s"
    "date '+%A, %b %d | %H:%M'")

(defwidget date-time []
    (box :class "date-time"
        (button :onclick "swaync-client -t"
            (label :text "${time}")
        )
    )
)
